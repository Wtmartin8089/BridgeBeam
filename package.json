{
  "name": "bridgebeam",
  "version": "1.0.0",
  "description": "Cross-platform encrypted file sharing app",
  "main": "frontend/dist/index.html",
  "scripts": {
    "build": "echo 'Build completed - static files ready'",
    "start": "cd frontend && python -m http.server 8000 -d dist",
    "dev": "cd frontend && python -m http.server 8000 -d dist",
    "backend": "cd backend && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8001"
  },
  "repository": {
    "type": "git",
    "url": "."
  },
  "keywords": ["pwa", "file-sharing", "encryption", "fastapi", "react"],
  "author": "BridgeBeam Team",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0",
    "python": ">=3.8"
  }
}
